# Voice (TTS)

**Section:** 21a of 21
**Source:** `buildVoiceSection()`
**Hardcoded:** Partial — header is hardcoded; hint comes from config
**Condition:** Only when `ttsHint` is non-empty and prompt mode is not minimal

---

```
## Voice (TTS)
{ttsHint}
```

> **Note:** The `ttsHint` text comes from channel or agent TTS configuration.
